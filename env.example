# 微信运动步数自动更新工具配置文件
# 复制此文件为 .env 并填入你的信息

# Zepp账户信息（必填）
email=你的邮箱@example.com
password=你的密码

# 步数配置（可选）
total_step=7000                      # 每日基础目标步数，默认7000
delta=0.2                           # 随机偏差系数，默认0.2（即±20%的偏差）

# API地址配置（可选）
base_url=https://clound.gjshou.top/ # API服务器地址，默认为当前可用地址

# 配置说明：
# - total_step: 每日实际目标将在此值的50%-150%范围内随机变化
# - delta: 控制步数更新的随机性，值越大随机性越强
# - base_url: 如果API地址发生变更，可以修改此项
#   当前地址：https://clound.gjshou.top/ （默认值） 